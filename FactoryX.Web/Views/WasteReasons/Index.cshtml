@model IEnumerable<FactoryX.Application.DTOs.WasteReasonDto>

@{
    ViewData["Title"] = "أسباب الهالك والمرفوضات";
}

<div class="container-fluid py-4">
    <!-- Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="fw-bold mb-1 text-primary">
                <i class="bi bi-tags me-2"></i> دليل أسباب الهالك والمرفوضات
            </h2>
            <p class="text-muted mb-0">إدارة وتصنيف أسباب تلف الخامات، فاقد مراحل التشغيل، ومرفوضات الإنتاج التام</p>
        </div>
        <div class="d-flex gap-2">
            <a asp-controller="Waste" asp-action="Index" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-right me-1"></i> سجل الهوالك
            </a>
            <a asp-action="Create" class="btn btn-primary shadow-sm">
                <i class="bi bi-plus-circle me-1"></i> إضافة سبب جديد
            </a>
        </div>
    </div>

    <!-- Alert Messages -->
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show rounded-3 shadow-sm" role="alert">
            <i class="bi bi-check-circle-fill me-2"></i> @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show rounded-3 shadow-sm" role="alert">
            <i class="bi bi-exclamation-triangle-fill me-2"></i> @TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <!-- Filter Card -->
    <div class="card border-0 shadow-sm rounded-3 mb-4">
        <div class="card-body p-3">
            <div class="d-flex justify-content-between align-items-center flex-wrap gap-2">
                <div class="d-flex gap-2 align-items-center">
                    <a asp-action="Index" asp-route-onlyActive="false" class="btn btn-sm @(ViewBag.OnlyActive == false ? "btn-primary" : "btn-outline-secondary")">
                        الكل (@Model.Count())
                    </a>
                    <a asp-action="Index" asp-route-onlyActive="true" class="btn btn-sm @(ViewBag.OnlyActive == true ? "btn-primary" : "btn-outline-secondary")">
                        النشطة فقط (@Model.Count(r => r.IsActive))
                    </a>
                </div>
                <div class="text-muted small">
                    إجمالي الأسباب المسجلة: <strong class="text-dark">@Model.Count()</strong>
                </div>
            </div>
        </div>
    </div>

    <!-- Table Card -->
    <div class="card border-0 shadow-sm rounded-3">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th class="ps-4">كود السبب</th>
                        <th>اسم / عنوان السبب</th>
                        <th>الوصف والتفاصيل</th>
                        <th class="text-center">سجلات الهالك المرتبطة</th>
                        <th class="text-center">الحالة</th>
                        <th class="text-end pe-4">الإجراءات</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="6" class="text-center py-5 text-muted">
                                <i class="bi bi-inbox fs-1 d-block mb-3 text-secondary"></i>
                                لا توجد أسباب هالك مسجلة حالياً
                            </td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="ps-4">
                                    <span class="badge bg-light text-primary border fw-bold font-monospace fs-6">
                                        @item.Code
                                    </span>
                                </td>
                                <td class="fw-bold">
                                    <a asp-action="Details" asp-route-id="@item.Id" class="text-decoration-none text-dark">
                                        @item.Reason
                                    </a>
                                </td>
                                <td class="text-muted text-truncate" style="max-width: 300px;">
                                    @(string.IsNullOrEmpty(item.Description) ? "-" : item.Description)
                                </td>
                                <td class="text-center">
                                    <span class="badge bg-secondary-subtle text-secondary rounded-pill px-3 py-1">
                                        @item.WastesCount سجل
                                    </span>
                                </td>
                                <td class="text-center">
                                    @if (item.IsActive)
                                    {
                                        <span class="badge bg-success-subtle text-success rounded-pill px-3 py-1">
                                            <i class="bi bi-check-circle me-1"></i> نشط
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-danger-subtle text-danger rounded-pill px-3 py-1">
                                            <i class="bi bi-x-circle me-1"></i> معطل
                                        </span>
                                    }
                                </td>
                                <td class="text-end pe-4">
                                    <div class="btn-group btn-group-sm">
                                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-secondary" title="عرض التفاصيل">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-primary" title="تعديل">
                                            <i class="bi bi-pencil"></i>
                                        </a>
                                        <form asp-action="ToggleActive" asp-route-id="@item.Id" method="post" class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn @(item.IsActive ? "btn-outline-warning" : "btn-outline-success")" 
                                                    title="@(item.IsActive ? "تعطيل" : "تفعيل")"
                                                    onclick="return confirm('هل أنت متأكد من تغيير حالة تفعيل هذا السبب؟');">
                                                <i class="bi @(item.IsActive ? "bi-slash-circle" : "bi-check-circle")"></i>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
